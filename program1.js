var user = {};				//object
user.name = 'John';			//attribute `name` is added with value `John`
user.surname = 'Mike';		//attribute `suername` is added with value `Mike`
user.name = 'Peter';		//value of name is updated to `Peter`
delete user.name;			//attribute `name` is deleted from the object ; only surname attribute with value `Mike` is retained